---
name: "Rule1: QTY_GR_CWT_TOP_B_LENGTH > 0 and TYP_CWT_GR = TK5A"
priority: 1
condition: "request.qtyGrCwtTopBLength != null && request.qtyGrCwtTopBLength > 0 && request.typCwtGr != null && request.typCwtGr == 'TK5A'"
actions:
  - "result.result = 'KMC741050H02/F'; result.matchedRule = 'Rule1';"

---
name: "Rule2: QTY_GR_CWT_TOP_B_LENGTH > 0 and TYP_CWT_GR = HT60-1.5"
priority: 2
condition: "request.qtyGrCwtTopBLength != null && request.qtyGrCwtTopBLength > 0 && request.typCwtGr != null && request.typCwtGr == 'HT60-1.5'"
actions:
  - "result.result = 'KMC51051908V003/A'; result.matchedRule = 'Rule2';"

---
name: "Rule3: QTY_GR_CWT_TOP_B_LENGTH > 0 and TYP_CWT_GR = HT60"
priority: 3
condition: "request.qtyGrCwtTopBLength != null && request.qtyGrCwtTopBLength > 0 && request.typCwtGr != null && request.typCwtGr == 'HT60'"
actions:
  - "result.result = 'KMC51054042V003/B'; result.matchedRule = 'Rule3';"

---
name: "Rule4: QTY_GR_CWT_TOP_B_LENGTH > 0 and TYP_CWT_GR = HG65-2.0"
priority: 4
condition: "request.qtyGrCwtTopBLength != null && request.qtyGrCwtTopBLength > 0 && request.typCwtGr != null && request.typCwtGr == 'HG65-2.0'"
actions:
  - "result.result = 'KMC51606436V003/-'; result.matchedRule = 'Rule4';"

---
name: "Rule5: QTY_GR_CWT_TOP_B_LENGTH > 0 and TYP_CWT_GR = HG65"
priority: 5
condition: "request.qtyGrCwtTopBLength != null && request.qtyGrCwtTopBLength > 0 && request.typCwtGr != null && request.typCwtGr == 'HG65'"
actions:
  - "result.result = 'KMC51419319V000/A'; result.matchedRule = 'Rule5';"

---
name: "Default NA"
priority: 1000
condition: "true"
actions:
  - "result.result = 'NA'; result.matchedRule = 'No matching rule';"
